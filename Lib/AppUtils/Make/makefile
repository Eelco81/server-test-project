# Make a static library

SOURCEDIR       = ./../Src
LIBDIR          = ./../..
TOOLDIR         = ./../../../Tools/
SOURCES         = $(wildcard $(SOURCEDIR)/*.cpp)
DEPENDENCIES    = Core
LINKS           = -lpthread
LIBTARGET       = libapputils.a

-include $(TOOLDIR)/build.mk