

/*
#ifndef _ROUTES_H_
#define _ROUTES_H_

#include <memory>

namespace API {
    class Router;
}

class Routes {
public:
    Routes () = default;
    Routes (const Routes& inRoutes) = default;
    ~Routes () = default;

public:
    static void SetupEndpoints (std::shared_ptr <API::Router> ioRouter);
};


#endif

*/
