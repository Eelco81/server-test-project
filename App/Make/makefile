# create an executable

SOURCEDIR       = ./../Src
LIBDIR          = ./../../Lib
TOOLDIR         = ./../../Tools
SOURCES         = $(wildcard $(SOURCEDIR)/*.cpp)
DEPENDENCIES    = AppUtils TcpServer Router Xml Core
LINKS           = -lpthread
TARGET          = serveraplication

-include $(TOOLDIR)/build.mk